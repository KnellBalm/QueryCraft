# Dev 환경 설정 (내부 워크스테이션용)
# 복사해서 사용: cp .env.dev.example .env.dev

# PostgreSQL (내부 서버)
POSTGRES_DSN=host=192.168.101.224 user=postgres password=YOUR_PASSWORD dbname=querycraft port=5432

# Gemini API (테스트용)
GEMINI_API_KEY=your_gemini_api_key_here
USE_GEMINI=1

# Analytics (개발 시 비활성화)
VITE_MIXPANEL_TOKEN=dummy

# 환경 표시
ENV=development
