# Worker dependencies
# Shared with backend - ensuring all necessary modules are available for generation tasks

# Database
psycopg2-binary>=2.9.0,<3.0.0
duckdb>=0.9.0,<1.0.0

# Gemini AI
google-genai>=0.1.0

# Data processing
pandas>=2.0.0,<3.0.0
numpy>=1.24.0
# faker>=20.0.0  # Kept just in case, though currently unused in backend.generator

# Backend shared dependencies (needed for imports)
pydantic>=2.5.0,<3.0.0
fastapi>=0.109.0,<1.0.0
uvicorn>=0.27.0,<1.0.0
python-dotenv>=1.0.0,<2.0.0
tqdm>=4.0.0
pytz>=2023.3
httpx>=0.24.0
aiohttp>=3.9.0
bcrypt>=4.0.0,<5.0.0
